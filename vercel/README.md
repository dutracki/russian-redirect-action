# Vercel Edge Middleware

Use Vercel Middleware (Next.js) to intercept and redirect traffic.

## Prerequisites
- A Next.js application hosted on Vercel.

## Installation
1. Create a `middleware.js` (or `middleware.ts`) file at the root of your project.
2. Paste the contents of `middleware.js`.
3. Deploy to Vercel.
